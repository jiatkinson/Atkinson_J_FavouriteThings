<section id="favourites">

  {{#each vacay}}
  <div class="vacation">
    <h2 class="vacation-name">{{vacation_name}}</h2>
    <a href="/{{vacation_name}}">
        <img src="/images/{{vacation_img}}" alt="vaction image" class="vacation_img"></a>
  </div>
  {{/each}}

</section>